<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lr.service.testdemo.dao">

    <insert id="userRegister" parameterType="com.lr.service.testdemo.pojo.User">
        INSERT INTO t_user(
                    id,
                    name,
                    age,
                    pwd
        )VALUES (
                    #{id},
                    #{name},
                    #{age},
                    #{pwd}
        );
    </insert>


</mapper>